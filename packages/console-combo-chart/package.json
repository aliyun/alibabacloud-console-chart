{
  "name": "@alicloud/console-combo-chart",
  "version": "0.1.0-alpha.1",
  "main": "lib/index.js",
  "module": "es/index.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib",
    "es",
    "dist"
  ],
  "scripts": {
    "clean-rpt": "rm -rf .rpt2_cache",
    "clean": "rm -rf lib es dist",
    "compile": "rm -rf es && npx tsc --outDir es",
    "build": "npm run clean && rollup -c",
    "prepare": "npm run build",
    "watch": "rollup -c -w"
  },
  "dependencies": {
    "@alicloud/console-shared-utils": "^0.1.0-alpha.1",
    "tslib": "^1.10.0"
  }
}
