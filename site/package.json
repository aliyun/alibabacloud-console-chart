{
  "name": "gatsby-theme-console-doc-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "gatsby clean",
    "dev": "npm run clean && gatsby develop",
    "start": "npm run develop",
    "debug": "npm run clean && npx --node-arg=--inspect-brk gatsby develop",
    "build": "npm run clean && gatsby build --prefix-paths",
    "push-build": "npm run build && gh-pages -d public"
  },
  "dependencies": {},
  "devDependencies": {
    "@alicloud/gatsby-theme-console-doc": "^1.0.2",
    "gatsby": "^2.18.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "styled-components": "^4.4.1",
    "typescript": "^3.7.2",
    "gh-pages": "^2.1.1"
  }
}
